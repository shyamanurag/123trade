# ULTRA-AGGRESSIVE EXCLUSION - MINIMAL BUILD CONTEXT

# Exclude everything by default, then whitelist only essentials
*

# Allow only essential files
!app.py
!requirements.txt
!src/
!config/

# But exclude unnecessary subdirectories even from allowed paths
src/**/__pycache__/
src/**/*.pyc
src/**/*.log
config/**/*.log
config/**/*.db 